using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum HeightLevel { Ground, Ground2, Tall, VeryTall, Aerial };
public enum ElementType { None, Magic, Fire, Electric, Bleed };

public enum WeaponType { Archers, Cannon, HeavyBallista  };

public enum Direction { Up, Down, Left, Right };

public enum WeaponStat {  damage, fireRate, projectileSpeed, pierce, knockback, range, element };

public class Enums_System : MonoBehaviour
{
}
